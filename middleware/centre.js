// const { db } = require("../database/db");
// const CentreModel = db.Centre;

// const isValidCentreId = async () => {
//   try {
//     const validCentreId = await CentreModel.findOne({
//       raw: true,
//       where: {
//         id: centre_id,
//       },
//       attributes: ["centre"],
//     });
//     if(validCentreId && validCentreId)
//   } catch (error) {

//   }
// };

// module.exports = isValidCentreId;
